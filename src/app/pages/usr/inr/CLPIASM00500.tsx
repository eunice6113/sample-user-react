import * as React from "react";
import { BasePage } from "../../../shared/components/base/BasePage";
import './CLPIASM00500.css';

interface IProps {
    children: React.ReactNode;
}
// 클라우드 서비스 소개
const CLPIASM00500: React.FC<IProps> = ({children}) => {

    return(
    <BasePage>
        <h1></h1>
    </BasePage>)
}
export default CLPIASM00500;